defmodule OurHome.Seeds do
  @moduledoc """
  Database seed data. All actions should be idempotent since this module may be
  executed each time the server is started.
  """

  def call() do
  end
end
